# Start

One command — starts the server and opens fullscreen (kiosk) Firefox:

```bash
/home/caddy/PRODUCTION/start.sh
```

To exit: **Ctrl+Q** (quit kiosk) or **Ctrl+W** (close window). The Flask server stops automatically when the browser closes.
